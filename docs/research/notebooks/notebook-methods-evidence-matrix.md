# Notebook-to-Methods Evidence Matrix

## Purpose

This matrix maps manuscript preprocessing claims to specific legacy notebooks or scripts. It is the canonical provenance layer for notebook-grounded methods text.

## Evidence Matrix

| Preprocessing area | Primary notebook or script | Inputs evidenced | Outputs evidenced | Verified actions | Unresolved or limited points | Manuscript support level |
| --- | --- | --- | --- | --- | --- | --- |
| GDSC1 drug-response preparation | `GDSC/GDSC1_drug_response_v2_337K_V1.ipynb` | `Drug_list...csv`, GDSC response tables, curated drug lists | `GDSC1_drug_response_314403x9_v2.txt`, `GDSC_drug_response_drugs_w_smiles_988cl_404dr_337K_v4.txt`, pIC50 exports | renames dirty drug-metadata headers; filters to `Datasets == GDSC1`; deduplicates compounds by `Name` or `Synonyms`; filters by matched `Drug Id`; writes GDSC response tables with SMILES and pIC50-derived outputs | exact raw-response to pIC50 conversion code is not fully isolated in the sampled cells; notebook appears multi-stage and partially manual | direct manuscript support |
| GDSC2 drug-response preparation | `GDSC/GDSC2_drug_response_v2_337K_V1.ipynb` | `Drug_list...csv`, GDSC response tables | dataset-specific GDSC2 filtered tables | same header cleaning, dataset filtering, and deduplication pattern as GDSC1 | exact final export chain was not fully sampled in this pass | direct manuscript support |
| CCLE NP24 response preprocessing | `CCLE/CCLE_drug_response_v2.ipynb` | `CCLE_NP24.2009_Drug_data_2015.02.24.csv` | `CCLE_NP24_2009_Drug_data_drug_response_format_v1.txt` | selects `Compound`, `Primary Cell Line Name`, `IC50 (uM)`; renames to canonical `DRUG_NAME`, `CELL_LINE_NAME`, `IC50`; converts micromolar IC50 to pIC50 using `-(log10(IC50 / 1000000))`; writes long-format table | no additional QC filters were visible in sampled cells | direct manuscript support |
| CCLE GNF/NP26 response preprocessing | `CCLE/CCLE_drug_response_v2.ipynb` | `CCLE_GNF_data_090613.xls` | `CCLE_GNF_NP26_data_090613_drug_response_format.txt` | drops non-response columns; strips cell-line column whitespace; reshapes wide-format response values into a long table; computes pIC50 from IC50; writes prepared table | some steps remain inside commented or exploratory cells; notebook mixes NP24 and GNF flows | direct manuscript support with caveat |
| FIMM response preprocessing | `FIMM/FIMM_drug_response_dataset_v1.ipynb` | `41586_2016_BFnature20171_MOESM61_ESM.xlsx` | `FIMM_drug_response_data_2561x5_v2.txt` | remaps specific cell-line aliases (`RPMI` -> `RPMI-8226`, `RMG1` -> `RMG-I`); fits dose-response curves with `curve_fit`; extracts IC50 as parameter `c`; converts IC50 to pIC50; sets `METRIC = pIC50`; renames to canonical fields | no explicit failure-handling for failed curve fits was visible in sampled cells | direct manuscript support |
| NCI-60 response preprocessing | `NCI-60/NCI-60_drug_response_dataset_v1.ipynb` | `DTP_NCI60_RAW.zip` / `DTP_NCI60_RAW.xlsx` | `NCI-60_actual_drug_response_4418389x8_v1.txt`, `NCI-60_missing_drug_response_673271x8_v1.txt` | preserves source framing as negative log10 GI50; separates drug metadata from wide response table; derives cell-line names and tissues by splitting colon-delimited column names; reshapes to long format; tags `METRIC = pGI50`; separates actual from missing values after replacing `na` with sentinel `9999` | sentinel-based missing-value handling should be described as notebook-specific, not idealized as final best practice | direct manuscript support |
| NCATS response preprocessing | `NCATS/NCATS_drug_response_v1.ipynb` | `data_NCATS IC50_act.txt`, `Cell_line_annotation_tncats.txt` | `NCATS_drug_response_v1.txt` | reshapes wide cell-line response columns into long format by repeated concatenation; maps tissue from annotation table; renames final fields to `COMPOUND_NAME`, `CELL_LINE_NAME`, `VALUE`, `METRIC`, `DATASET`, `TISSUE` | notebook labels response column as `pIC50` but later sets `METRIC = IC50`; this inconsistency must be stated explicitly in methods caveats | direct manuscript support with caveat |
| Cell-line harmonization and Cellosaurus enrichment | `NCATS/FIMM_id_matching_for_csaurus_v4.ipynb` and related cell-line notebooks | `cellosaurus_cell_line_output.csv`, dataset-specific cell-line tables | enriched cell-line tables with synonym, accession, disease, comments, references | constructs dictionary-based mappings from Cellosaurus identifiers and synonyms to accession, disease, references, comments, cross-references; extracts tissue-like annotations from Cellosaurus comments using regex on `Derived from site ... UBERON`; maps enriched metadata into dataset tables | fuzzy matching behavior is not yet directly evidenced from sampled cells; some corrections appear manual and notebook-specific | direct manuscript support |
| Compound normalization and fingerprint generation | `CLRB_MODELLING/Fingerprint_production_for_approved_compounds_v1.ipynb` and SMILES/InChI notebooks | `Mathced_compounds_for_SMILES_v1.txt` and mapped SMILES tables | `Prediction_compounds_SMILES_FPS_df_v1.txt` | loads compound-SMILES mappings; computes Morgan/ECFP4 fingerprints with radius 2 and 1024 bits using RDKit; stores bit strings in an `ECFP4` column | broader compound synonym and InChI resolution is spread across multiple notebooks and not yet fully consolidated in one scripted pipeline | direct manuscript support |
| Predictor input assembly | `CLRB_MODELLING/Prediction_pairs_vector_matrice_preparation_v1.ipynb` | GDSC, CCLE, and NCI-60 3747-feature matrices; cell-line mapping tables; merged compound tables | assembled prediction matrices for downstream inference | loads GDSC/CCLE/NCI-60 vectors in a shared 3747-feature space; maps cell-line IDs to canonical names; attaches RRIDs from Cellosaurus-derived lookups; intersects datasets across shared feature space and compound lists | upstream generation of the 3747-feature matrices is not reconstructed here; notebook shows consumption and cross-platform alignment rather than full omics preprocessing | direct manuscript support |
| Model training and external testing | `CLRB_MODELLING/GDSC_CCLE_cross_domain_mode_7.py` plus `GDSC_trained_model_predictions_for_all_platforms_v1.ipynb` | `GDSC_drug_response_drugs_w_smiles_988cl_404dr_337K_v4_w_fps.txt`, `CCLE_drug_response_df_for_cross_domain_v3.txt`, GDSC/CCLE 3747-feature matrices | `GDSC_CCLE_cross_domain_mode_7_v3.joblib`, prediction tables, metric text file | sorts by edited cell-line/drug IDs; splits 1024-bit fingerprint strings into columns; merges fingerprints with omics vectors; builds 4771-feature matrices; casts to `float32`; trains RandomForest on GDSC and evaluates on CCLE | script consumes preserved prepared files and does not expose upstream omics normalization/feature-selection details | direct manuscript support |

## Interpretation Rules

- Use this matrix as the source of truth for manuscript preprocessing claims.
- If a preprocessing step is only visible as a final prepared file or a consumed intermediate matrix, describe it as preserved provenance rather than fully reconstructed implementation.
- When notebook behavior is inconsistent, state the inconsistency explicitly instead of normalizing it silently in prose.
