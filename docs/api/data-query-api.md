# Data Query API

## Status

Planned contract for repository documentation and future implementation.

## Core Endpoints

- `GET /datasets`
- `GET /cell-lines`
- `GET /compounds`
- `GET /targets`
- `GET /network`

## Contract Rules

- return canonical identifiers where possible
- expose source dataset provenance
- include pagination and filter echoing in responses
- version the API explicitly once implementation begins
