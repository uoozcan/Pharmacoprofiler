name: frontend-tests

on:
  push:
    branches: [main, develop]
  pull_request:

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: test -f app/frontend/package.json || exit 0
      - run: cd app/frontend && npm ci
      - run: cd app/frontend && npm test -- --runInBand
